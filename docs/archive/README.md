# docs/archive — superseded & historical

These documents captured earlier product directions, proposals, or plans that have
since **shipped, been rejected, or been superseded.** They're kept only for
decision history — **none of them describes the current product.**

For current truth, see the living docs — which now live in the **memex**, not this
repo: `memex-vault/wiki/projects/rotli/model.md` (the canonical conceptual model)
and `memex-vault/wiki/projects/rotli/main-brain-daemon.md` (the Main / Brain /
organizer spec, contract v3.7). The repo keeps only the README + this archive.

| File | Why it's archived |
|---|---|
| `memex-write-contract-v3.5-proposal.md` | Strawman for a separate visible `notes/` root that was **rejected**; the write contract is now **v3.7** (`wiki/_inbox` staging won). |
| `memex-rules-first-pass.md` | Self-marked superseded by the ratified v3.6 write contract; still used the retired `self/` root (now `identity/` + `personality/`). |
| `next-stages.md` | Track 1 (Chat MVP) has **shipped** — Chat is a live front; Track 2 (memex multi-root) was superseded by the `corpus.json` unification. |
| `notes-chat-inbox-rearchitecture.md` | The three-fronts (Notes · Chat · Inbox) rearchitecture it proposed is now **live** — see `../model.md`. |
| `roadmap.html` | A 2026-06-13 snapshot (six fronts, a Memory surface, `Brain · Storage` destinations, "six appearances") that no longer holds. |
| `testing-map.html` | Stage-1 manual QA checklist built against the retired `Inbox · Brain · Storage · Archive · Trash` destination model. |
