# Feedback sweep — 2026-07-03

Working tracker for the 26-item feedback note (`memex-vault wiki/_inbox/rotli-feedback-rq86ya.md`).
Shipped in 7 subsystem batches, each its own version bump. Seth GUI-tests each batch before the next.

**Decisions (2026-07-03):** ⌘D = duplicate selection/line, split-right rebinds to ⌘\ ·
chat sidebar cap 5/10/15 (default 5) · batched delivery.

Status legend: ⬜ todo · 🔨 in progress · ✅ done · 🧪 awaiting Seth's GUI test

---

## Batch 1 · Tabs & Main core — `v0.25.0`
- 🧪 **#8** ⌘T → new *blank* note, not a duplicate — `keys/actions.ts` `newNoteInTab`, `TabStrip.tsx`; retired `panes.ts` `newTab()`
- 🧪 **#15** every new note lands in **Main** — `keys/actions.ts` `fileNewNoteIntoMain` + `addNoteToMainAt()`
- 🧪 **#16** new tab inside a Main folder opens in that same folder — via the active note's Main folder (`mainParentOfNote`), no pane state needed
- 🧪 **#12a** new-**tab** hotkey acts where you are (⌘T files into the current Main folder) — done here
- ⬜ **#12b** new-**folder** hotkey → deferred to Batch 2 (lands with the IDE-style new-folder icon rework)

*Validation: `tsc` clean · 379 tests pass (added `addNoteToMainAt` / `mainParentOfNote` coverage) · check:hex ok. Awaiting Seth's GUI pass.*

## Batch 2 · Sidebar structure & navigation — DONE 2026-07-03 (merged `ad9cb29`)
Built in an isolated worktree (wf/batch2-sidebar, commit 7d6594d); independent review = SHIP.
- ✅ **#17** chat list default 5 — new `chatSidebarLimit` (5/10/15) setting; retired `CHAT_SECTION_LIMIT=12`
- ✅ **#9** stars-column whitespace — `.main-row .snact { margin-left:0 }` so the title grows + star/× ride flush
- ✅ **#25** current file `.sel` highlight on Main rows + auto-expand its folder (Main chain wins, else reveal in Brain); reveal-only, never fights a manual collapse
- ✅ **#7 + #13** IDE-style toolbar icons (New note/folder/board · Collapse all) + per-folder new-note/new-folder hover icons; the `+` dropdown is gone (`NewFileGlyph`/`NewFolderGlyph`). The inline name-first input is KEPT (a folder must be nameable — #16), just driven by the unambiguous icons.
- 🟡 **#14 + #26** SUBSTRATE shipped (`src/state/navHistory.ts` back/forward store, wired through `panes.openNote`, tested); the ‹ › buttons + moving the filter to the **wordmark titlebar** + the top-center search dropdown were **DEFERRED** (need the wordmark bar built first + a new search surface; would crowd the 190px rail). Own follow-up — the nav store is ready to plug in.
- Validation: tsc · **413 tests** (merged) · hex · cargo 157 — all green.

## Batch 3 · Editor — images — `v0.27.0`
- ⬜ **#1** crisp downscaling — `ImgWidget` in `livePreview.ts`
- ⬜ **#10** live drag-in preview + drag placed images around
- ⬜ **#22** clicking an image selects it, doesn't reveal markdown source
- ⬜ **#3** highlight-over-image styling — `editor.css`

## Batch 4 · Editor — text, tables, links — `v0.28.0`
- ⬜ **#2** ⌘D duplicate; split-right → ⌘\ — `keys/actions.ts` + new editor command
- ⬜ **#18** table polish: fully editable beautified mode, raw markdown hidden — `tableRender.ts`
- ⬜ **#19** links become hyperlinks on space/enter — `livePreview.ts`
- ⬜ **#20** Settings: default browser for links — `openUrl` + settings

## Batch 5 · Files & spreadsheets — `v0.29.0`
- ⬜ **#4** xlsx/csv editable — edit/read mode toggle, double-click cell → edit — `FileSurface.tsx`, `SheetEditor.tsx`
- ⬜ **#24** right-click Show in Finder on notes (disabled/relabeled for drive-hosted) — wire `corpus_reveal_file`

## Batch 6 · Chat — `v0.30.0`
- ⬜ **#11** chat header metadata to top-right, out of content column + subtle last-updated — `ChatSurface.tsx`
- ⬜ **#21** in-file chat button + hotkey → right pane pre-fed with the current note — `chatSummon.ts`, `EditorSurface.tsx`

## Batch 7 · Onboarding & Metadata — `v0.31.0`
- ⬜ **#5** stable Continue button, location maintained — `Onboarding.tsx`
- 🧪 **#6** dedup "Keep current" vs same-path memex option — `Onboarding.tsx` (pulled forward 2026-07-03: `found` drops any detected memex whose path == current location; tsc + 379 tests green)
- 🧪 **#23** metadata icon → **instant toggle**; Lock/Secure/File-to-Brain moved to the note right-click menu; `MetaPanel` popover deleted; **"★ In Main"** indicator added to the editor header (pulled forward 2026-07-03; tsc + 379 tests + hex green). Also resolves the #1b "nothing shows it's in Main" gap.

## Batch 8 · Subfolder-aware Brain filing — (Rust, `organizer.rs`)
Seth (2026-07-03): the organizer filed rotli-feedback to `wiki/projects` but not the `projects/rotli` subfolder — it classifies to top-level AREAS only. Investigated; own batch.
- ⬜ Offer existing subfolders as candidate targets in the classify prompt — `organizer.rs area_vocab` / `classify_prompt`
- ⬜ Extend `ClassifyOut` with `subfolder: Option<String>` + validate against real subfolders — `organizer.rs parse_classify`
- ⬜ Build the move target `wiki/<area>/<subfolder>/`; lift the `/`-in-area rejection — `corpus.rs:2522` `file_note()`
- ⬜ Journal row records the subfolder (undo/audit) — `organizer.rs JournalRow`
- ⬜ TS mirror `fileNoteToArea(noteId, area, subfolder?)` — `brainFiling.ts`
- Note: the Filer write-gate already permits `wiki/**`; `area_members` already walks recursively (indexes stay correct). No contract bump needed.

## Batch 9 · Organizer controls — DONE (2026-07-03)
Seth: watch a file, wait ~5 min after last edit, then organize; and let me choose the organizing AI (claude -p Sonnet 5 for now).
- ✅ **Idle delay** — default quiet window 45s → **300s (5 min)**; `organizerQuietSecs` knob + Settings picker (1/2/5/10/15 min). `organizer.rs DEFAULT_QUIET`.
- ✅ **Organizer model setting** — `organizerModel` (`local` default | `claude`); Rust transport dispatches to `provider::organizer_claude_complete` (`claude -p` Sonnet, reuses `run_registered`). Secure/locked never sent remotely. `ui.ts` · `persist.ts` · `SettingsSurface BrainPane`.
- Validation: **154 Rust + 379 TS tests · tsc · hex** all green. *Note: default stays local; Seth flips his to Claude Sonnet 5 in Settings → Brain after updating (the old build would drop the key on quit).*

## Breve check-up spam — FIXED (2026-07-03)
- ✅ Root cause: `import_file` copied dropped files into `storage/` verbatim → spaced `storage:` links that fail the memex validator regex. Now slugified on import (`corpus.rs sanitize_asset_name` + test). Existing rotli-feedback note's 16 refs + files de-spaced; `validate.ts` passes.

## Post-Batch-2 follow-ups (2026-07-03, on `main`, pending release 0.24.4)
- ✅ **Tab model fixed** — plain click REUSES the open tab, else new tab, never replaces (standard editor model). `panes.ts placeTab` + 4 tests.
- ✅ **Note location indicator** — clickable "where this lives" chip (★ Main · area), reveals + scrolls to the note in the sidebar. `lib/noteLocation.ts` (6 tests) + `ui.revealFocusedNote` + sidebar reveal/scroll split. Notes → Main from All Notes via row right-click today; drag-from-grid = follow-up.
- ⬜ **Clean-up Room** — DESIGN written (`docs/design/cleanup-room.md`): a guided triage surface above Brain (chat left · queue right) over stale/low-confidence/unfiled/orphan notes; human-in-the-loop twin of the organizer, on the Breve-merge path. 4 open decisions (name · delete=Trash · staleness defaults · trigger). Build C0→C3 not started.

## EPIC · Breve → rotli merge (decided 2026-07-03, design pending)
Seth chose a **FULL merge**: rotli absorbs Breve entirely (Signal I/O, on-device TTS, PDF+email, launchd scheduling, watchlist, email triage, routines/briefs), rename Breve→rotli, archive the Breve repo. Sequenced AFTER organizer controls + the 0.24.3 release.
- ✅ **Design doc written (2026-07-03): `docs/design/breve-merge.md`** — full spec grounded in a verbatim Breve inventory (2 deep explorer passes). Seth's three hard constraints baked in: (1) `bun run check` + `cargo test` green at the end, (2) never lose the briefs or watchlist (migration checklist §8), (3) Signal is manual in Settings with a Twilio→Signal walkthrough (§6). Reuses rotli's existing infra (provider.rs lanes = the brief fallback chain; organizer worker → routines scheduler; AI-Models-pane detection). Bun routines-engine sidecar preserves Breve's tested TS. 5-phase build plan, migration + archive plan.
- ✅ **4 decisions resolved** (breve-merge.md §11): Bun sidecar · watchlist=memex note · opt-in-with-detection · read-only email.
- ✅ **P0 · Foundations DONE 2026-07-03** (merged `ad9cb29`; wf/breve-p0 commit 775b3a3; review = SHIP): `src/routines/` (types · pure tz-aware `nextRun` · `parseWatchlist`) + 19 tests; additive Rust `provider.rs provider_chain` (first-success chain) + 3 tests. Nothing wired into the live daemon yet (P1). Green: 413 TS + 157 Rust.
- ⬜ Build P1→P5 (P1 = scheduler + Settings→Routines + morning brief to an in-app note; watchlist migrated).

## Delivered out of order (2026-07-03, same day as Batch 1)
- **#6** (onboarding dedup) + **#23** (metadata instant-toggle + In-Main indicator) + the Breve fix + **Batch 9** all ride the `0.24.3` release.
