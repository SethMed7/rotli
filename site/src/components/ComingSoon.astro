---
// The holding page for rotli.co before launch: what Rotli is, in one screen,
// with no download and no account. It is pinned to the Rotli light environment
// (no toggle, no saved preference) so every visitor sees the same poster.
import Base from '../layouts/Base.astro';
import waving from '../../../src/assets/characters/waving.svg?raw';
import SiteFooter from './SiteFooter.astro';
import SiteHeader from './SiteHeader.astro';

const promises = [
  {
    title: 'One folder is the truth.',
    detail:
      'Markdown stays Markdown. Documents, sheets, images, and boards keep their ordinary formats. Delete the app and the work still makes sense.',
  },
  {
    title: 'A workspace, not a viewer.',
    detail:
      'Write, plan, draw, and chat in one calm window. Summon it from anywhere, and keep working when the network is gone.',
  },
  {
    title: 'AI with a boundary.',
    detail:
      'Start with an on-device model, connect the model tools you already trust, or use none. Secure notes never leave your Mac.',
  },
];
---

<Base
  title="Rotli — coming soon"
  description="Rotli is a warm, local-first Mac workspace for notes, editable files, boards, and AI conversations. One folder you own. Coming soon."
  canonicalPath="/"
  lockAppearance={{ family: 'rotli', mode: 'light' }}
>
  <SiteHeader github={false} toggle={false} />

  <main id="content" class="soon">
    <section class="soon-copy" aria-labelledby="soon-title">
      <p class="kicker">Coming soon for macOS</p>
      <h1 id="soon-title">Room to think.<br />Files you keep.</h1>
      <p class="lede">
        Rotli is a warm, local-first workspace that turns one ordinary folder on your Mac into a
        calm place for notes, editable files, boards, and AI conversations. No account. No cloud
        copy you did not ask for.
      </p>
      <ul class="promises">
        {promises.map((promise, index) => (
          <li>
            <span class="index" aria-hidden="true">0{index + 1}</span>
            <p><b>{promise.title}</b> {promise.detail}</p>
          </li>
        ))}
      </ul>
      <p class="status">
        <span class="status-mark" aria-hidden="true"></span>
        Public beta on its way · Free · Apple silicon · no account required
      </p>
    </section>

    <figure class="soon-art" aria-hidden="true">
      <div class="art-field" set:html={waving}></div>
    </figure>
  </main>

  <SiteFooter tagline="A local-first workspace for your Mac. © 2026 Rotli" />

  <style>
    /* Mobile first: one column, a compact quokka above the copy. */
    .soon {
      width: min(100% - 2rem, var(--page-max));
      margin-inline: auto;
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 1.5rem;
      padding-block: 2.5rem 3.5rem;
    }

    .soon-art {
      order: -1;
      margin: 0;
    }

    .art-field {
      width: 8.5rem;
      color: var(--text);
    }

    .art-field :global(svg) {
      display: block;
      width: 100%;
      height: auto;
    }

    .soon-copy {
      max-width: 38rem;
    }

    .kicker {
      margin: 0 0 0.9rem;
      color: var(--accent-text);
      font-size: 0.88rem;
      font-weight: 600;
      letter-spacing: 0.02em;
    }

    h1 {
      margin: 0;
      font-size: clamp(2.6rem, 11vw, 5.4rem);
      line-height: 0.98;
      letter-spacing: -0.04em;
      font-weight: 600;
    }

    .lede {
      max-width: 34rem;
      margin: 1.25rem 0 0;
      color: var(--text-muted);
      font-size: clamp(1.02rem, 1.6vw, 1.25rem);
      line-height: 1.55;
    }

    .promises {
      display: grid;
      gap: 0.85rem;
      margin: 2rem 0 0;
      padding: 0;
      list-style: none;
    }

    .promises li {
      display: grid;
      grid-template-columns: 2rem minmax(0, 1fr);
      gap: 0.4rem;
      padding-top: 0.85rem;
      border-top: 1px solid var(--border);
    }

    .promises .index {
      color: var(--accent-text);
      font-size: 0.8rem;
      font-weight: 600;
      line-height: 1.9;
    }

    .promises p {
      margin: 0;
      color: var(--text-muted);
      font-size: 0.96rem;
    }

    .promises b {
      color: var(--text);
      font-weight: 600;
    }

    .status {
      display: flex;
      align-items: flex-start;
      gap: 0.6rem;
      margin: 2rem 0 0;
      color: var(--text-muted);
      font-size: 0.88rem;
      line-height: 1.5;
    }

    .status-mark {
      flex: none;
      width: 0.6rem;
      height: 0.6rem;
      margin-top: 0.4rem;
      border-radius: 999px;
      border: 1px solid var(--accent-text);
      background: var(--accent);
    }

    /* Tablet: more air, larger quokka, still one column. */
    @media (min-width: 640px) {
      .soon {
        width: min(100% - 3rem, var(--page-max));
        gap: 2rem;
        padding-block: 3.5rem 4.5rem;
      }

      .art-field {
        width: 11rem;
      }
    }

    /* Desktop: copy beside the quokka in its field, vertically centred. */
    @media (min-width: 960px) {
      .soon {
        grid-template-columns: minmax(0, 1.15fr) minmax(16rem, 0.85fr);
        gap: clamp(2.5rem, 6vw, 6rem);
        align-items: center;
        min-height: calc(100vh - 76px - 8rem);
        min-height: calc(100svh - 76px - 8rem);
        padding-block: clamp(3.5rem, 8vw, 7rem);
      }

      .soon-art {
        order: 0;
      }

      .art-field {
        width: 100%;
        max-width: 26rem;
        aspect-ratio: 1;
        margin-inline: auto;
        padding: 12%;
        border: 1px solid var(--border);
        border-radius: 1rem;
        background: var(--surface-2);
      }

      .art-field :global(svg) {
        height: 100%;
      }
    }
  </style>
</Base>
