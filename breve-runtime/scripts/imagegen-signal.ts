#!/usr/bin/env bun
/**
 * BREVE image generation over Signal — "img: <description>".
 * Provider execution is paused; this command fails before resolving a binary.
 */
console.error("ERR cloud image generation is unavailable; no provider account was used");
process.exit(1);
